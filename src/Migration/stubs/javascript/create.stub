const { Migration, Migration, Schema } = require("elegance-orm");

module.exports = class {{ class }} extends Migration {
    async up() {
        await Schema.create("{{ table }}", table => {
            
        });
    }

    async down() {
        await Schema.dropIfExists("{{ table }}");
    }
};
