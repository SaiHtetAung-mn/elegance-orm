const { Migration, MigrationInterface, Schema } = require("elegance-orm");

module.exports = class {{ class }} implements MigrationInterface {
    async up() {
        await Schema.table("{{ table }}", table => {
            
        });
    }

    async down() {
        await Schema.table("{{ table }}", table => {
            
        });
    }
};
