import { Migration } from "mini-orm";

export default class {{ class }} extends Migration {
    async up(): Promise<void> {
        await this.schema.create("{{ table }}", table => {
            table.id();
            table.timestamps();
        });
    }

    async down(): Promise<void> {
        await this.schema.dropIfExists("{{ table }}");
    }
}
