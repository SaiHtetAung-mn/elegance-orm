import { Migration, Migration, Schema } from "elegance-orm";

export default class {{ class }} extends Migration {
    async up(): Promise<void> {
        await Schema.table("{{ table }}", table => {
            // Update schema changes
        });
    }

    async down(): Promise<void> {
        await Schema.table("{{ table }}", table => {
            // Revert schema updates
        });
    }
}
